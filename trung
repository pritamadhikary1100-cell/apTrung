-- ==================== AUTO HATCH MODULE ====================
-- Load từ: https://raw.githubusercontent.com/maihoangphi2531/REPO/main/autohatch.lua
-- Yêu cầu: WindUI, các biến global từ file chính
-- ===========================================================

local Module = {}

-- ✅ Lấy các biến cần thiết từ file chính
local WindUI = _G.WindUI
local Window = _G.Window
local Players = game:GetService("Players")
local c = _G.PlayerLocal
local d = _G.UserId
local e = _G.HttpService
local ConfigSystem = _G.ConfigSystem
local Toggles = _G.Toggles
local Options = _G.Options
local bQ = _G.GearPerks  -- Perk data
local bR = _G.ItemsModule  -- Items data

-- ✅ Lấy ReplicatedStorage references
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Shared = ReplicatedStorage:WaitForChild("Shared")
local ItemsModule = require(Shared:WaitForChild("Items"))
local BuyEggRemote = Shared:WaitForChild("Pets"):WaitForChild("BuyEgg")
local EquipItemRemote = Shared:WaitForChild("Inventory"):WaitForChild("EquipItem")
local HatchRemote = Shared:WaitForChild("Pets"):WaitForChild("Hatch")

-- ✅ Lấy Player references
local PlayerGui = c:WaitForChild("PlayerGui")
local Profile = PlayerGui:WaitForChild("Profile")
local InventoryItems = Profile:WaitForChild("Inventory"):WaitForChild("Items")
local PlayerEquips = ReplicatedStorage:WaitForChild("PlayerEquips"):WaitForChild(c.Name)
local PetEquip = PlayerEquips:WaitForChild("Pet")

-- ==================== GLOBALS ====================
local HatchGlobals = {
    isAutoHatching = false,
    isAutoBuying = false,
    selectedEgg = "",
    purchaseMethod = "Gold", -- "Gold" hoặc "Gems"
    eggsList = {},
    totalEggsBought = 0,
    totalEggsHatched = 0,
    sessionStartTime = 0,
    smartPetKeep = false, -- Toggle smart pet keep
    autoSellPetByTier = false, -- Toggle auto sell pet by tier
    autoSellAllPets = false, -- Toggle auto sell all pets
    petPerkThresholds = {}, -- Ngưỡng perk cho pet (riêng biệt với weapon)
    petTierSellList = {true, true, true, true, true}, -- Tier nào sẽ bị bán (1-5)
    totalPetsSold = 0, -- Thống kê số pet đã bán
    -- Config
    ConfigFile = "AnyaHub_HatchConfig_" .. c.Name .. ".txt"
}

-- ==================== UTILITY FUNCTIONS ====================
local function libNoti(msg, duration)
    WindUI:Notify({
        Title = "Auto Hatch",
        Content = msg,
        Duration = duration or 5
    })
end

local function formatNumber(num)
    if num >= 1000000 then
        return string.format("%.1fM", num / 1000000)
    elseif num >= 1000 then
        return string.format("%.1fK", num / 1000)
    else
        return tostring(num)
    end
end

local function timeElapsed(seconds)
    local hours = math.floor(seconds / 3600)
    local mins = math.floor((seconds % 3600) / 60)
    local secs = seconds % 60
    
    if hours > 0 then
        return string.format("%dh %dm %ds", hours, mins, secs)
    elseif mins > 0 then
        return string.format("%dm %ds", mins, secs)
    else
        return string.format("%ds", secs)
    end
end

-- ==================== EGG FUNCTIONS ====================
-- Lấy danh sách eggs từ Items module
local function getEggsList()
    local eggs = {}
    
    for itemName, itemData in pairs(ItemsModule) do
        -- Kiểm tra xem có phải là egg không (thường có Type = "Egg" hoặc tên chứa "Egg")
        if itemData.Type == "Egg" or string.match(itemName, "Egg") then
            table.insert(eggs, itemName)
        end
    end
    
    table.sort(eggs)
    
    -- Nếu không tìm thấy eggs, thêm một số eggs phổ biến
    if #eggs == 0 then
        eggs = {
            "StarterEgg",
            "ForestEgg", 
            "DesertEgg",
            "ArcticEgg",
            "VolcanoEgg",
            "OceanEgg",
            "SpaceEgg",
            "MythicEgg",
            "LegendaryEgg",
            "StarEgg"
        }
    end
    
    return eggs
end

-- Mua egg
local function buyEgg(eggName, method)
    if not eggName or eggName == "" then
        return false
    end
    
    local success = pcall(function()
        BuyEggRemote:FireServer(eggName, method or "Gold")
    end)
    
    if success then
        HatchGlobals.totalEggsBought = HatchGlobals.totalEggsBought + 1
    end
    
    return success
end

-- Tìm tất cả eggs trong inventory
local function findEggsInInventory()
    local eggs = {}
    
    for _, item in ipairs(InventoryItems:GetChildren()) do
        -- Kiểm tra xem item có phải là egg không
        local itemName = item.Name
        if string.match(itemName, "Egg") or (ItemsModule[itemName] and ItemsModule[itemName].Type == "Egg") then
            table.insert(eggs, item)
        end
    end
    
    return eggs
end

-- Equip egg vào Pet slot
local function equipEgg(eggItem)
    if not eggItem or not eggItem.Parent then
        return false
    end
    
    local success = pcall(function()
        EquipItemRemote:FireServer(eggItem, PetEquip)
    end)
    
    return success
end

-- Hatch egg đã được equip
local function hatchEquippedEgg()
    local success = pcall(function()
        -- Lấy vị trí của player để hatch
        local character = c.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            local position = character.HumanoidRootPart.Position
            HatchRemote:FireServer(position)
        end
    end)
    
    if success then
        HatchGlobals.totalEggsHatched = HatchGlobals.totalEggsHatched + 1
    end
    
    return success
end

-- Hatch một egg: Equip -> Hatch -> Wait
local function hatchSingleEgg(eggItem)
    -- Bước 1: Equip egg
    if not equipEgg(eggItem) then
        return false
    end
    
    task.wait(0.2) -- Đợi equip xong
    
    -- Bước 2: Hatch egg đã equip
    local success = hatchEquippedEgg()
    
    task.wait(0.3) -- Đợi animation hatch
    
    return success
end

-- Hatch tất cả eggs trong inventory
local function hatchAllEggs()
    local eggs = findEggsInInventory()
    local hatchedCount = 0
    
    for _, egg in ipairs(eggs) do
        if hatchSingleEgg(egg) then
            hatchedCount = hatchedCount + 1
            task.wait(2) -- Delay nhỏ giữa các lần hatch
        end
    end
    
    return hatchedCount
end

-- ==================== SMART PET KEEP ====================
-- Lấy danh sách pet perks (GIỐNG LOGIC GAME: PetOnly=true HOẶC Universal)
local function getPetPerksList()
    local petPerks = {}
    
    for perkKey, perkData in pairs(bQ) do
        -- ✅ LOGIC GIỐNG GAME: RollForPerk line 268-277
        -- Điều kiện: (không có WeaponOnly và không có ArmorOnly) HOẶC (có PetOnly)
        local canAppearOnPet = (not perkData.WeaponOnly and not perkData.ArmorOnly) or perkData.PetOnly
        
        if canAppearOnPet and not perkData.Disabled then
            -- Ưu tiên PetStatRange, nếu không có thì dùng StatRange
            local statRange = perkData.PetStatRange or perkData.StatRange
            
            if statRange then
                table.insert(petPerks, {
                    key = perkKey,
                    name = perkData.DisplayName,
                    desc = perkData.DisplayDesc,
                    min = statRange[1] * 100,
                    max = statRange[2] * 100
                })
            end
        end
    end
    
    table.sort(petPerks, function(a, b) return a.name < b.name end)
    return petPerks
end

-- Kiểm tra pet có perk đạt ngưỡng không (GIỐNG VŨ KHÍ - CHỈ CẦN 1 PERK ĐẠT)
local function checkPetPerks(petItem)
    local hasValidPerk = false
    local bestPerkInfo = nil
    local shouldKeepPet = false
    
    -- ✅ Loop qua tất cả children để tìm Perk, Perk1, Perk2, Perk3
    for _, child in ipairs(petItem:GetChildren()) do
        -- Chỉ xử lý nếu là StringValue có PerkValue (tức là Perk hoặc Perk1/2/3)
        if child:IsA("StringValue") and child:FindFirstChild("PerkValue") then
            local perkName = child.Value
            local perkValue = child.PerkValue.Value
            
            -- Lấy perk data
            local perkData = bQ[perkName]
            
            -- ✅ KIỂM TRA PERK HỢP LỆ: Phải có thể xuất hiện trên pet
            -- Logic giống getPetPerksList: (không WeaponOnly và không ArmorOnly) HOẶC có PetOnly
            if perkData then
                local canAppearOnPet = (not perkData.WeaponOnly and not perkData.ArmorOnly) or perkData.PetOnly
                local statRange = perkData.PetStatRange or perkData.StatRange
                
                if canAppearOnPet and statRange then
                    hasValidPerk = true
                    
                    -- Lấy ngưỡng từ HatchGlobals.petPerkThresholds
                    -- ✅ NẾU KHÔNG CÓ THRESHOLD -> Dùng 999% (chắc chắn BÁN)
                    local threshold = HatchGlobals.petPerkThresholds[perkName]
                    if not threshold then
                        warn(string.format("⚠️ Pet có perk '%s' nhưng KHÔNG CÓ trong slider! -> Sẽ BÁN", perkName))
                        threshold = 999 -- Ngưỡng cao để chắc chắn BÁN
                    else
                        -- ✅ CONVERT STRING SANG NUMBER (config lưu dưới dạng string!)
                        threshold = tonumber(threshold) or 999
                    end
                    
                    local perkPercent = math.round(perkValue * 100)
                    
                    -- ✅ Nếu perk này đạt ngưỡng -> GIỮ PET
                    local meetsThreshold = perkPercent >= threshold
                    
                    if meetsThreshold and not shouldKeepPet then
                        shouldKeepPet = true
                    end
                    
                    -- Lưu perk tốt nhất để hiển thị (perk đầu tiên đạt ngưỡng hoặc perk cao nhất)
                    if not bestPerkInfo or meetsThreshold or (not shouldKeepPet and perkPercent > bestPerkInfo.value) then
                        bestPerkInfo = {
                            name = perkData.DisplayName,
                            value = perkPercent,
                            threshold = threshold,
                            meets = meetsThreshold
                        }
                    end
                end
            end
        end
    end
    
    -- Nếu không có perk hợp lệ nào -> BÁN
    if not hasValidPerk then
        return false, {
            name = "No Valid Perk",
            value = 0,
            threshold = 0
        }
    end
    
    return shouldKeepPet, bestPerkInfo
end

-- Sell pet
local function sellPet(petItem)
    if not petItem then
        return false
    end
    
    -- ✅ Dùng chung remote SellItems như vũ khí
    local SellRemote = ReplicatedStorage:WaitForChild("Shared"):WaitForChild("Drops"):WaitForChild("SellItems")
    local success = pcall(function()
        SellRemote:InvokeServer({petItem})
    end)
    
    if success then
        HatchGlobals.totalPetsSold = HatchGlobals.totalPetsSold + 1
    end
    
    return success
end

-- Lấy tier của pet
local function getPetTier(petItem)
    if not petItem or not petItem:FindFirstChild("Variant") then
        return nil
    end
    
    local petName = petItem.Name
    local variantValue = petItem.Variant.Value
    
    -- Lấy data từ ItemsModule
    local itemData = ItemsModule[petName]
    if not itemData or itemData.Type ~= "Pet" or not itemData.Variants then
        return nil
    end
    
    -- Tìm variant matching
    for _, variant in ipairs(itemData.Variants) do
        -- variant = {variantId, name, weight, tier, skill}
        if variant[1] == variantValue then
            return variant[4] -- Tier ở vị trí thứ 4
        end
    end
    
    return nil
end

-- Kiểm tra xem pet có nên bị sell theo tier không
local function shouldSellPetByTier(petItem)
    local tier = getPetTier(petItem)
    if not tier then
        return false -- Không xác định được tier, không sell
    end
    
    -- Kiểm tra xem tier này có trong danh sách sell không
    return HatchGlobals.petTierSellList[tier] == true
end

-- Lấy tất cả pets trong inventory
local function getAllPetsInInventory()
    local pets = {}
    
    for _, item in ipairs(InventoryItems:GetChildren()) do
        if item:IsA("Folder") then
            local itemData = bR[item.Name]
            if itemData and itemData.Type == "Pet" then
                table.insert(pets, item)
            end
        end
    end
    
    return pets
end

-- Bán tất cả pets trong inventory
local function sellAllPets()
    local pets = getAllPetsInInventory()
    local soldCount = 0
    
    if #pets == 0 then
        return 0
    end
    
    -- Thu thập tất cả pets vào một array
    local petsToSell = {}
    for _, pet in ipairs(pets) do
        table.insert(petsToSell, pet)
    end
    
    -- Gọi remote để bán tất cả cùng lúc
    if #petsToSell > 0 then
        -- ✅ Dùng chung remote SellItems như vũ khí
        local SellRemote = ReplicatedStorage:WaitForChild("Shared"):WaitForChild("Drops"):WaitForChild("SellItems")
        local success = pcall(function()
            SellRemote:InvokeServer(petsToSell)
        end)
        
        if success then
            soldCount = #petsToSell
            HatchGlobals.totalPetsSold = HatchGlobals.totalPetsSold + soldCount
        end
    end
    
    return soldCount
end

-- ==================== AUTO FUNCTIONS ====================
local function startAutoBuy()
    if HatchGlobals.isAutoBuying then
        warn("⚠️ Auto Buy đã đang chạy!")
        return
    end
    
    if not HatchGlobals.selectedEgg or HatchGlobals.selectedEgg == "" then
        libNoti("❌ Vui lòng chọn egg trước!", 3)
        return
    end
    
    HatchGlobals.isAutoBuying = true
    if HatchGlobals.sessionStartTime == 0 then
        HatchGlobals.sessionStartTime = tick()
    end
    
    libNoti("✅ Bắt đầu Auto Buy: " .. HatchGlobals.selectedEgg, 3)
    
    task.spawn(function()
        while HatchGlobals.isAutoBuying do
            local success = buyEgg(HatchGlobals.selectedEgg, HatchGlobals.purchaseMethod)
            
            if not success then
                task.wait(1)
            else
                task.wait(0.5) -- Fixed delay
            end
        end
        
        libNoti("⏹️ Đã dừng Auto Buy", 3)
    end)
end

local function startAutoHatch()
    if HatchGlobals.isAutoHatching then
        warn("⚠️ Auto Hatch đã đang chạy!")
        return
    end
    
    HatchGlobals.isAutoHatching = true
    if HatchGlobals.sessionStartTime == 0 then
        HatchGlobals.sessionStartTime = tick()
    end
    
    libNoti("✅ Bắt đầu Auto Hatch", 3)
    
    task.spawn(function()
        while HatchGlobals.isAutoHatching do
            local hatchedCount = hatchAllEggs()
            
            if hatchedCount == 0 then
                task.wait(2) -- Đợi lâu hơn nếu không có egg
            else
                task.wait(0.5) -- Fixed delay
            end
        end
        
        libNoti("⏹️ Đã dừng Auto Hatch", 3)
    end)
end

-- ==================== CONFIG FUNCTIONS ====================
local function saveHatchConfig()
    if not writefile then return end
    
    -- CHỈ lưu stats riêng từng acc (không lưu petPerkThresholds - nó sẽ lưu qua ConfigSystem)
    local data = {
        totalEggsBought = HatchGlobals.totalEggsBought,
        totalEggsHatched = HatchGlobals.totalEggsHatched
    }
    
    pcall(function()
        writefile(HatchGlobals.ConfigFile, e:JSONEncode(data))
    end)
    
    warn("💾 Đã lưu Hatch stats (riêng acc)")
end

local function loadHatchConfig()
    if not (isfile and readfile and isfile(HatchGlobals.ConfigFile)) then
        return false
    end
    
    local success = pcall(function()
        local data = e:JSONDecode(readfile(HatchGlobals.ConfigFile))
        
        -- CHỈ load stats riêng từng acc
        HatchGlobals.totalEggsBought = data.totalEggsBought or 0
        HatchGlobals.totalEggsHatched = data.totalEggsHatched or 0
    end)
    
    if success then
        warn("📂 Đã load Hatch stats (riêng acc)")
    end
    
    return success
end

-- ==================== UI CREATION ====================
local function createAutoHatchTab()
    local TabHatch = Window:Tab({
        Title = "Auto Hatch",
        Icon = "egg",
        Locked = false
    })
    
    TabHatch:Section({ Title = "🥚 Auto Egg System" })
    
    TabHatch:Divider({ Text = "Egg Selection" })
    
    -- Lấy danh sách eggs
    HatchGlobals.eggsList = getEggsList()
    
    -- Dropdown chọn egg
    local EggDropdown = TabHatch:Dropdown({
        Title = "📋 Chọn Egg",
        Description = "Chọn loại egg muốn mua",
        Values = HatchGlobals.eggsList,
        Multi = false,
        Default = HatchGlobals.selectedEgg ~= "" and {HatchGlobals.selectedEgg} or nil,
        Callback = function(selected)
            -- WindUI trả về table với 1 phần tử khi Multi = false
            local eggName = type(selected) == "table" and selected[1] or selected
            if eggName and eggName ~= "" then
                HatchGlobals.selectedEgg = eggName
                HatchGlobals.purchaseMethod = "Gold" -- Luôn dùng Gold
                -- KHÔNG gọi saveHatchConfig() vì selectedEgg sẽ được lưu qua ConfigSystem
                warn("✅ Đã chọn egg:", HatchGlobals.selectedEgg)
                libNoti("✅ Đã chọn: " .. eggName, 2)
            end
        end
    })
    ConfigSystem.registerUIElement("selectedEgg", EggDropdown)
    
    TabHatch:Divider({ Text = "Auto Settings" })
    
    -- Auto buy toggle
    local AutoBuyToggle = TabHatch:Toggle({
        Title = "🛒 Auto Buy Egg",
        Description = "Tự động mua egg đã chọn",
        Default = false,
        Callback = function(state)
            -- ✅ Cập nhật qua Toggles wrapper để ConfigSystem nhận được
            if _G.Toggles and _G.Toggles.AutoBuyEgg then
                _G.Toggles.AutoBuyEgg:SetValue(state)
            end
            
            if state then
                startAutoBuy()
            else
                HatchGlobals.isAutoBuying = false
            end
        end
    })
    ConfigSystem.registerUIElement("AutoBuyEgg", AutoBuyToggle)
    
    -- Auto hatch toggle
    local AutoHatchToggle = TabHatch:Toggle({
        Title = "🐣 Auto Hatch Egg",
        Description = "Tự động nở tất cả egg trong inventory",
        Default = false,
        Callback = function(state)
            -- ✅ Cập nhật qua Toggles wrapper để ConfigSystem nhận được
            if _G.Toggles and _G.Toggles.AutoHatchEgg then
                _G.Toggles.AutoHatchEgg:SetValue(state)
            end
            
            if state then
                startAutoHatch()
            else
                HatchGlobals.isAutoHatching = false
            end
        end
    })
    ConfigSystem.registerUIElement("AutoHatchEgg", AutoHatchToggle)
    
    TabHatch:Divider({ Text = "Smart Pet Keep" })
    
    -- Smart Pet Keep Toggle
    local SmartPetKeepToggle = TabHatch:Toggle({
        Title = "🎯 Smart Pet Keep",
        Description = "Tự động giữ pet có perk đạt ngưỡng, bán còn lại",
        Default = false,
        Callback = function(state)
            -- ✅ Cập nhật qua Toggles wrapper để ConfigSystem nhận được
            if _G.Toggles and _G.Toggles.SmartPetKeep then
                _G.Toggles.SmartPetKeep:SetValue(state)
            end
            HatchGlobals.smartPetKeep = state
        end
    })
    ConfigSystem.registerUIElement("SmartPetKeep", SmartPetKeepToggle)
    
    -- ✅ AUTO SELL PET BY TIER
    TabHatch:Divider({ Text = "Auto Sell by Tier" })
    
    local AutoSellPetByTierToggle = TabHatch:Toggle({
        Title = "🗑️ Auto Sell Pet by Tier",
        Description = "Tự động bán pet theo tier đã chọn",
        Default = false,
        Callback = function(state)
            if _G.Toggles and _G.Toggles.AutoSellPetByTier then
                _G.Toggles.AutoSellPetByTier:SetValue(state)
            end
            HatchGlobals.autoSellPetByTier = state
            
            if state then
                libNoti("✅ Bật Auto Sell Pet by Tier", 2)
            else
                libNoti("⏹️ Tắt Auto Sell Pet by Tier", 2)
            end
        end
    })
    ConfigSystem.registerUIElement("AutoSellPetByTier", AutoSellPetByTierToggle)
    
    -- Tier checkboxes (giống weapon tier)
    local tierNames = {"Tier 1", "Tier 2", "Tier 3", "Tier 4", "Tier 5"}
    local tierToggles = {}
    
    for tier = 1, 5 do
        local tierToggle = TabHatch:Toggle({
            Title = "💎 " .. tierNames[tier],
            Description = "Bán pet " .. tierNames[tier],
            Default = true, -- Mặc định bán tất cả tier
            Callback = function(state)
                -- ✅ Update qua Toggles wrapper
                if _G.Toggles and _G.Toggles["PetTierSell_" .. tier] then
                    _G.Toggles["PetTierSell_" .. tier]:SetValue(state)
                end
                HatchGlobals.petTierSellList[tier] = state
                warn(string.format("📊 Pet Tier %d sell: %s", tier, tostring(state)))
            end
        })
        tierToggles[tier] = tierToggle
        ConfigSystem.registerUIElement("PetTierSell_" .. tier, tierToggle)
    end
    
    TabHatch:Divider({ Text = "Smart Pet Keep Thresholds" })
    
    -- ✅ TẠO SLIDER CHO TỪNG PET PERK
    local petPerks = getPetPerksList()
    for _, perk in ipairs(petPerks) do
        -- Khởi tạo giá trị mặc định
        if not HatchGlobals.petPerkThresholds[perk.key] then
            HatchGlobals.petPerkThresholds[perk.key] = math.round(perk.min)
        end
        
        local slider = TabHatch:Slider({
            Title = perk.name,
            Description = perk.desc .. string.format(" (Range: %.1f%% - %.1f%%)", perk.min, perk.max),
            Step = 1,
            Value = {
                Min = math.round(perk.min),
                Max = math.round(perk.max),
                Default = HatchGlobals.petPerkThresholds[perk.key]
            },
            Callback = function(value)
                HatchGlobals.petPerkThresholds[perk.key] = value
                warn(string.format("📊 Set %s threshold: %.1f%%", perk.name, value))
            end
        })
        -- ✅ ĐĂNG KÝ SLIDER với prefix "PetPerk_" để tránh trùng với weapon perk
        ConfigSystem.registerUIElement("PetPerk_" .. perk.key, slider)
        warn(string.format("✅ Registered UI: PetPerk_%s", perk.key))
    end
    
    -- ✅ AUTO SELL PET KHI HATCH (Smart Pet Keep listener)
    InventoryItems.ChildAdded:Connect(function(item)
        task.wait(0.1) -- Đợi item load xong
        
        -- Kiểm tra xem có phải pet không
        local itemData = bR[item.Name]
        if not itemData or itemData.Type ~= "Pet" then
            return
        end
        
        -- ✅ PRIORITY 0: Auto Sell All Pets (nếu enabled, bán tất cả - bỏ qua mọi filter)
        if HatchGlobals.autoSellAllPets then
            if sellPet(item) then
                -- Không hiển thị noti cho từng con để tránh spam
            end
            return
        end
        
        -- Lấy tier của pet
        local petTier = getPetTier(item)
        
        -- ✅ PRIORITY 1: Smart Pet Keep CHỈ áp dụng cho TIER 5
        if HatchGlobals.smartPetKeep and petTier == 5 then
            local shouldKeep, perkInfo = checkPetPerks(item)
            
            if shouldKeep then
                -- Pet Tier 5 đạt ngưỡng -> GIỮ LẠI
                libNoti(string.format("✅ Giữ (T5): %s | %s: %.1f%% ≥ %.1f%%", 
                    itemData.DisplayKey or item.Name,
                    perkInfo.name,
                    perkInfo.value,
                    perkInfo.threshold
                ), 2)
                return -- Đã giữ, không cần check tier filter
            end
            -- Nếu Tier 5 không đạt ngưỡng, tiếp tục check tier filter bên dưới
        end
        
        -- ✅ PRIORITY 2: Auto Sell by Tier (bán theo tier đã chọn)
        if HatchGlobals.autoSellPetByTier then
            if shouldSellPetByTier(item) then
                if sellPet(item) then
                    libNoti(string.format("💰 Bán (Tier %d): %s", petTier or 0, itemData.DisplayKey or item.Name), 1.5)
                end
                return -- Đã sell
            end
        end
        
        -- ✅ PRIORITY 3: Smart Pet Keep cho các tier khác (nếu không bật Auto Sell by Tier)
        -- → Chỉ chạy khi Auto Sell by Tier TẮT hoặc pet không thuộc tier sell list
        if HatchGlobals.smartPetKeep then
            local shouldKeep, perkInfo = checkPetPerks(item)
            if not shouldKeep then
                -- Pet không đạt ngưỡng → Bán
                if sellPet(item) then
                    libNoti(string.format("💰 Bán (Perk): %s | %s: %.1f%% < %.1f%%", 
                        itemData.DisplayKey or item.Name,
                        perkInfo.name,
                        perkInfo.value,
                        perkInfo.threshold
                    ), 2)
                end
            end
        end
    end)
    
    TabHatch:Divider({ Text = "Manual Actions" })
    
    -- Toggle Auto Sell All Pets
    local AutoSellAllPetsToggle = TabHatch:Toggle({
        Title = "🗑️ Auto Sell All Pets",
        Description = "Tự động bán tất cả pets (bỏ qua tất cả filter khác)",
        Default = false,
        Callback = function(state)
            if _G.Toggles and _G.Toggles.AutoSellAllPets then
                _G.Toggles.AutoSellAllPets:SetValue(state)
            end
            HatchGlobals.autoSellAllPets = state
            
            if state then
                libNoti("⚠️ Bật Auto Sell ALL Pets - Sẽ bán tất cả!", 3)
            else
                libNoti("⏹️ Tắt Auto Sell All Pets", 2)
            end
        end
    })
    ConfigSystem.registerUIElement("AutoSellAllPets", AutoSellAllPetsToggle)
    
    -- Button Sell All Pets
    TabHatch:Button({
        Title = "🗑️ Sell All Pets",
        Description = "Bán tất cả pets trong inventory (nhấn đúp)",
        Callback = function()
            local count = sellAllPets()
            if count > 0 then
                libNoti(string.format("💰 Đã bán %d pets!", count), 3)
            else
                libNoti("⚠️ Không có pet trong inventory", 3)
            end
        end
    })
    
    TabHatch:Divider({ Text = "Other Actions" })
    
    -- Button mua 1 egg
    TabHatch:Button({
        Title = "🛒 Mua 1 Egg",
        Description = "Mua 1 egg thủ công",
        Callback = function()
            if not HatchGlobals.selectedEgg or HatchGlobals.selectedEgg == "" then
                libNoti("❌ Vui lòng chọn egg trước!", 3)
                return
            end
            
            local success = buyEgg(HatchGlobals.selectedEgg, HatchGlobals.purchaseMethod)
            if success then
                libNoti("✅ Đã mua 1 " .. HatchGlobals.selectedEgg, 2)
            else
                libNoti("❌ Không thể mua egg!", 3)
            end
        end
    })
    
    -- Button hatch all
    TabHatch:Button({
        Title = "🐣 Hatch All Eggs",
        Description = "Nở tất cả egg trong inventory (nhấn đúp)",
        Callback = function()
            local count = hatchAllEggs()
            if count > 0 then
                libNoti("✅ Đã hatch " .. count .. " eggs", 3)
            else
                libNoti("⚠️ Không có egg trong inventory", 3)
            end
        end
    })
    
    -- Button reset stats
    TabHatch:Button({
        Title = "🔄 Reset Statistics",
        Description = "Reset thống kê session",
        Callback = function()
            HatchGlobals.totalEggsBought = 0
            HatchGlobals.totalEggsHatched = 0
            HatchGlobals.sessionStartTime = tick()
            saveHatchConfig()
            libNoti("✅ Đã reset thống kê", 2)
        end
    })
    
    TabHatch:Divider({ Text = "Statistics" })
    
    -- Statistics paragraphs
    local StatsPara1 = TabHatch:Paragraph({
        Title = "📊 Session Stats",
        Description = "Loading..."
    })
    
    local StatsPara2 = TabHatch:Paragraph({
        Title = "🥚 Eggs in Inventory",
        Description = "Loading..."
    })
    
    -- Update stats task
    task.spawn(function()
        while true do
            local elapsed = tick() - HatchGlobals.sessionStartTime
            local stats = string.format(
                "⏱️ Thời gian: %s\n" ..
                "🛒 Đã mua: %s eggs\n" ..
                "🐣 Đã hatch: %s eggs\n" ..
                "💰 Đã bán: %s pets",
                timeElapsed(math.floor(elapsed)),
                formatNumber(HatchGlobals.totalEggsBought),
                formatNumber(HatchGlobals.totalEggsHatched),
                formatNumber(HatchGlobals.totalPetsSold)
            )
            
            StatsPara1:SetDesc(stats)
            
            local eggsInInv = #findEggsInInventory()
            StatsPara2:SetDesc(string.format("%d eggs", eggsInInv))
            
            task.wait(1)
        end
    end)
    
    -- Load config
    loadHatchConfig()
end

-- ==================== INIT MODULE ====================
function Module:Init()
    -- Load config cá nhân trước (stats riêng từng acc)
    loadHatchConfig()
    
    -- Tạo UI
    createAutoHatchTab()
    
    -- ✅ SAU KHI TẠO UI, TỰ ĐỘNG LOAD CONFIG VÀ UPDATE UI
    task.spawn(function()
        task.wait(0.5) -- Đợi UI render xong
        
        warn("🔄 Auto Hatch: Attempting to sync UI with config...")
        
        -- Lấy config hiện tại từ ConfigSystem
        local currentConfig = nil
        if ConfigSystem.selectedConfig and ConfigSystem.selectedConfig ~= "" then
            local configPath = ConfigSystem.ConfigFolder .. "/" .. ConfigSystem.selectedConfig .. ".txt"
            if isfile and readfile and isfile(configPath) then
                local success, data = pcall(function()
                    return e:JSONDecode(readfile(configPath))
                end)
                if success and data then
                    currentConfig = data
                    warn("✅ Found existing config:", ConfigSystem.selectedConfig)
                end
            end
        end
        
        -- Nếu có config, load và update UI
        if currentConfig and currentConfig.HatchGlobals then
            warn("📂 Loading HatchGlobals from config...")
            
            -- Load selectedEgg
            if currentConfig.HatchGlobals.selectedEgg then
                HatchGlobals.selectedEgg = currentConfig.HatchGlobals.selectedEgg
                warn("✅ Loaded selectedEgg:", HatchGlobals.selectedEgg)
                
                -- Update dropdown
                local dropdown = ConfigSystem.UIElements["selectedEgg"]
                if dropdown and dropdown.SetValue then
                    pcall(function()
                        dropdown:SetValue({HatchGlobals.selectedEgg})
                        warn("✅ Updated selectedEgg dropdown")
                    end)
                end
            end
            
            -- Load petPerkThresholds
            if currentConfig.HatchGlobals.petPerkThresholds then
                warn("📂 Loading Pet Perk Thresholds...")
                for key, value in pairs(currentConfig.HatchGlobals.petPerkThresholds) do
                    local numValue = tonumber(value) or value
                    
                    -- Loại bỏ prefix "PetPerk_" khi lưu vào HatchGlobals
                    local perkKey = key
                    if string.sub(key, 1, 8) == "PetPerk_" then
                        perkKey = string.sub(key, 9)
                    end
                    
                    HatchGlobals.petPerkThresholds[perkKey] = numValue
                    warn(string.format("✅ Loaded threshold %s: %.1f", perkKey, numValue))
                    
                    -- Update slider
                    local sliderKey = "PetPerk_" .. perkKey
                    local slider = ConfigSystem.UIElements[sliderKey]
                    if slider then
                        local success, err = pcall(function()
                            -- ✅ WindUI slider dùng method :Set(value)
                            if slider.Set and type(slider.Set) == "function" then
                                slider:Set(numValue)
                                warn(string.format("✅ Set %s threshold: %.1f%%", perkKey, numValue))
                            else
                                warn(string.format("⚠️ Slider %s không có method :Set()", sliderKey))
                            end
                        end)
                        
                        if not success then
                            warn(string.format("❌ Error updating slider %s: %s", sliderKey, tostring(err)))
                        end
                    else
                        warn(string.format("⚠️ Slider not found: %s", sliderKey))
                    end
                end
            end
        else
            warn("ℹ️ No existing config found, using defaults")
        end
    end)
    
    -- Hook vào ConfigSystem để save/load selectedEgg (chung cho tất cả acc)
    local originalGetConfigData = ConfigSystem.getConfigData
    ConfigSystem.getConfigData = function()
        local data = originalGetConfigData()
        -- Lưu selectedEgg + petPerkThresholds vào config chung
        -- ✅ Chuyển đổi từ petPerkThresholds (không prefix) sang PetPerk_ (có prefix)
        local petPerkConfig = {}
        for perkKey, value in pairs(HatchGlobals.petPerkThresholds) do
            petPerkConfig["PetPerk_" .. perkKey] = value
        end
        
        data.HatchGlobals = {
            selectedEgg = HatchGlobals.selectedEgg,
            petPerkThresholds = petPerkConfig, -- Lưu với prefix
            petTierSellList = HatchGlobals.petTierSellList,
            autoSellAllPets = HatchGlobals.autoSellAllPets
        }
        return data
    end
    
    -- Hook vào loadConfigData để load selectedEgg và update UI
    local originalLoadConfigData = ConfigSystem.loadConfigData
    ConfigSystem.loadConfigData = function(data)
        -- ✅ GỌI ORIGINAL TRƯỚC
        originalLoadConfigData(data)
        
        warn("🔍 Module Auto Hatch: Checking config data...")
        
        -- Load selectedEgg + petPerkThresholds từ config
        if data and data.HatchGlobals then
            warn("📂 Found HatchGlobals in config!")
            
            if data.HatchGlobals.selectedEgg then
                HatchGlobals.selectedEgg = data.HatchGlobals.selectedEgg
                warn("✅ Loaded selectedEgg:", HatchGlobals.selectedEgg)
            end
            
            -- ✅ Load Pet Perk Thresholds từ config
            if data.HatchGlobals.petPerkThresholds then
                for key, value in pairs(data.HatchGlobals.petPerkThresholds) do
                    -- ✅ CONVERT STRING SANG NUMBER nếu cần
                    local numValue = tonumber(value) or value
                    
                    -- ✅ Loại bỏ prefix "PetPerk_" khi lưu vào HatchGlobals
                    local perkKey = key
                    if string.sub(key, 1, 8) == "PetPerk_" then
                        perkKey = string.sub(key, 9) -- Cắt bỏ 8 ký tự đầu ("PetPerk_")
                    end
                    
                    HatchGlobals.petPerkThresholds[perkKey] = numValue
                    warn(string.format("✅ Loaded threshold %s: %.1f", perkKey, numValue))
                end
                warn("✅ Đã load Pet Perk Thresholds từ config")
            end
            
            -- ✅ Load Pet Tier Sell List từ config
            if data.HatchGlobals.petTierSellList then
                HatchGlobals.petTierSellList = data.HatchGlobals.petTierSellList
                warn("✅ Đã load Pet Tier Sell List từ config:", e:JSONEncode(HatchGlobals.petTierSellList))
            end
            
            -- ✅ Load Auto Sell All Pets từ config
            if data.HatchGlobals.autoSellAllPets ~= nil then
                HatchGlobals.autoSellAllPets = data.HatchGlobals.autoSellAllPets
                warn("✅ Đã load Auto Sell All Pets từ config:", tostring(HatchGlobals.autoSellAllPets))
            end
            
            -- ✅ Update UI sau khi load (delay để UI đã render xong)
            task.spawn(function()
                -- ✅ ĐỢI UI ELEMENTS ĐƯỢC ĐĂNG KÝ (tối đa 10s)
                local maxWait = 10
                local waited = 0
                local uiElementsReady = false
                
                while waited < maxWait do
                    -- Kiểm tra xem đã có bất kỳ UI element nào được đăng ký chưa
                    local totalElements = 0
                    for _ in pairs(ConfigSystem.UIElements) do
                        totalElements = totalElements + 1
                    end
                    
                    -- Nếu có ít nhất 3 elements (selectedEgg + 2 toggles tối thiểu), coi như ready
                    if totalElements >= 3 then
                        warn(string.format("✅ Đã tìm thấy %d UI elements!", totalElements))
                        uiElementsReady = true
                        break
                    end
                    
                    task.wait(0.5)
                    waited = waited + 0.5
                    warn(string.format("⏳ Đang chờ UI elements... (%d found, waited %.1fs)", totalElements, waited))
                end
                
                if not uiElementsReady then
                    warn("⚠️ Timeout waiting for UI elements")
                    return
                end
                
                task.wait(0.5) -- Delay thêm chút để chắc chắn
                
                warn("🔄 Updating UI elements...")
                
                -- ✅ DEBUG: Liệt kê tất cả UI elements đã được đăng ký
                warn("📋 Danh sách UI elements đã đăng ký:")
                local elementCount = 0
                for key, element in pairs(ConfigSystem.UIElements) do
                    elementCount = elementCount + 1
                    warn(string.format("  - %s: %s", key, tostring(element)))
                end
                warn(string.format("📊 Tổng cộng: %d elements", elementCount))
                
                -- Update dropdown
                local dropdown = ConfigSystem.UIElements["selectedEgg"]
                if dropdown and HatchGlobals.selectedEgg ~= "" then
                    pcall(function()
                        dropdown.Value = {HatchGlobals.selectedEgg}
                        -- ❌ KHÔNG gọi callback vì có thể gây lỗi
                        warn("✅ Đã update UI dropdown với:", HatchGlobals.selectedEgg)
                    end)
                else
                    warn("⚠️ Dropdown not found or selectedEgg empty")
                end
                
                -- Update sliders
                warn("📊 Bắt đầu update sliders...")
                warn("📊 HatchGlobals.petPerkThresholds:", game:GetService("HttpService"):JSONEncode(HatchGlobals.petPerkThresholds))
                
                local slidersUpdated = 0
                local slidersNotFound = 0
                
                for perkKey, value in pairs(HatchGlobals.petPerkThresholds) do
                    -- ✅ TÌM SLIDER với prefix "PetPerk_"
                    local sliderKey = "PetPerk_" .. perkKey
                    local slider = ConfigSystem.UIElements[sliderKey]
                    
                    if slider then
                        local success, err = pcall(function()
                            local numValue = tonumber(value)
                            if not numValue then
                                warn(string.format("⚠️ Invalid value for %s: %s", sliderKey, tostring(value)))
                                return
                            end
                            
                            -- ✅ WindUI slider dùng method :Set(value)
                            if slider.Set and type(slider.Set) == "function" then
                                slider:Set(numValue)
                                slidersUpdated = slidersUpdated + 1
                                warn(string.format("✅ Updated slider %s: %.1f%%", sliderKey, numValue))
                            else
                                warn(string.format("⚠️ Slider %s không có method :Set()", sliderKey))
                            end
                        end)
                        
                        if not success then
                            slidersNotFound = slidersNotFound + 1
                            warn(string.format("❌ Failed to update slider %s: %s", sliderKey, tostring(err)))
                        end
                    else
                        slidersNotFound = slidersNotFound + 1
                        warn(string.format("⚠️ Slider not found: %s", sliderKey))
                    end
                end
                
                warn(string.format("📊 Kết quả: %d sliders updated, %d not found", slidersUpdated, slidersNotFound))
                
                -- Update toggles
                local autoBuyState = _G.ToggleStates.AutoBuyEgg
                local autoHatchState = _G.ToggleStates.AutoHatchEgg
                local smartPetKeepState = _G.ToggleStates.SmartPetKeep
                local autoSellPetByTierState = _G.ToggleStates.AutoSellPetByTier
                local autoSellAllPetsState = _G.ToggleStates.AutoSellAllPets
                
                local autoBuyToggle = ConfigSystem.UIElements["AutoBuyEgg"]
                if autoBuyToggle then
                    pcall(function()
                        local state = autoBuyState or false
                        autoBuyToggle.Value = state
                        -- ❌ KHÔNG gọi callback vì có thể gây lỗi
                        warn("✅ Đã update UI AutoBuyEgg:", state)
                    end)
                end
                
                local autoHatchToggle = ConfigSystem.UIElements["AutoHatchEgg"]
                if autoHatchToggle then
                    pcall(function()
                        local state = autoHatchState or false
                        autoHatchToggle.Value = state
                        warn("✅ Đã update UI AutoHatchEgg:", state)
                    end)
                end
                
                local smartPetKeepToggle = ConfigSystem.UIElements["SmartPetKeep"]
                if smartPetKeepToggle then
                    pcall(function()
                        local state = smartPetKeepState or false
                        smartPetKeepToggle.Value = state
                        HatchGlobals.smartPetKeep = state
                        warn("✅ Đã update UI SmartPetKeep:", state)
                    end)
                end
                
                local autoSellPetByTierToggle = ConfigSystem.UIElements["AutoSellPetByTier"]
                if autoSellPetByTierToggle then
                    pcall(function()
                        local state = autoSellPetByTierState or false
                        autoSellPetByTierToggle.Value = state
                        HatchGlobals.autoSellPetByTier = state
                        warn("✅ Đã update UI AutoSellPetByTier:", state)
                    end)
                end
                
                local autoSellAllPetsToggle = ConfigSystem.UIElements["AutoSellAllPets"]
                if autoSellAllPetsToggle then
                    pcall(function()
                        local state = autoSellAllPetsState or false
                        autoSellAllPetsToggle.Value = state
                        HatchGlobals.autoSellAllPets = state
                        warn("✅ Đã update UI AutoSellAllPets:", state)
                    end)
                end
                
                -- Update tier sell toggles
                for tier = 1, 5 do
                    local tierToggle = ConfigSystem.UIElements["PetTierSell_" .. tier]
                    if tierToggle then
                        pcall(function()
                            local state = HatchGlobals.petTierSellList[tier]
                            tierToggle.Value = state
                            warn(string.format("✅ Đã update UI PetTierSell_%d: %s", tier, tostring(state)))
                        end)
                    end
                end
                
                -- Start automation nếu cần
                if autoBuyState and not HatchGlobals.isAutoBuying then
                    warn("🔄 Đồng bộ AutoBuy từ config: true")
                    startAutoBuy()
                elseif not autoBuyState and HatchGlobals.isAutoBuying then
                    warn("🔄 Đồng bộ AutoBuy từ config: false")
                    HatchGlobals.isAutoBuying = false
                end
                
                if autoHatchState and not HatchGlobals.isAutoHatching then
                    warn("🔄 Đồng bộ AutoHatch từ config: true")
                    startAutoHatch()
                elseif not autoHatchState and HatchGlobals.isAutoHatching then
                    warn("🔄 Đồng bộ AutoHatch từ config: false")
                    HatchGlobals.isAutoHatching = false
                end
            end)
        else
            warn("⚠️ No HatchGlobals found in config data")
        end
    end
    
    warn("✅ Auto Hatch Module loaded!")
    libNoti("✅ Auto Hatch Module đã sẵn sàng!", 3)
end

return Module
